<Window x:Class="UI.VragenImporterenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Vragen importeren" Height="250" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Vragen importeren"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0"
                   Content="Onderwerp:"
                   Width="90"/>

            <ComboBox Grid.Column="1"
                      Name="ComboBoxOnderwerpen"
                      Height="25"/>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0"
                   Content="Bestand:"
                   Width="90"/>

            <TextBox Grid.Column="1"
                     Name="TextBoxPad"
                     Height="25"
                     IsReadOnly="True"/>

            <Button Grid.Column="2"
                    Content="Bladeren"
                    Width="90"
                    Margin="10,0,0,0"
                    Click="ButtonBladeren_Click"/>
        </Grid>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Annuleren"
                    Width="90"
                    Margin="5"
                    Click="ButtonAnnuleren_Click"/>

            <Button Content="Importeren"
                    Width="90"
                    Margin="5"
                    IsDefault="True"
                    Click="ButtonImporteren_Click"/>
        </StackPanel>
    </Grid>
</Window>