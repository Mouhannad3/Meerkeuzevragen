<Window x:Class="UI.TestUitvoerenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Test uitvoeren" Height="600" Width="750"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Test uitvoeren"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Test:" VerticalAlignment="Center"/>
            <ComboBox Grid.Column="1"
                      Name="ComboBoxTesten"
                      Height="25"
                      Margin="5,0,15,0"/>

            <Label Grid.Column="2" Content="GebruikerId:" VerticalAlignment="Center"/>
            <TextBox Grid.Column="3"
                     Name="TextBoxGebruikerId"
                     Height="25"
                     Margin="5,0,15,0"/>

            <Button Grid.Column="4"
                    Content="Test laden"
                    Width="90"
                    Click="ButtonTestLaden_Click"/>
        </Grid>

        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel Name="StackPanelVragen"/>
        </ScrollViewer>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="Sluiten"
                    Width="90"
                    Margin="5"
                    Click="ButtonSluiten_Click"/>

            <Button Content="Verbeteren"
                    Width="100"
                    Margin="5"
                    Click="ButtonVerbeteren_Click"/>
        </StackPanel>
    </Grid>
</Window>
