<Window x:Class="UI.TestUitvoerenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Test uitvoeren" Height="600" Width="700">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Test uitvoeren"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Test:"/>

            <ComboBox Grid.Column="1"
                      Name="ComboBoxTesten"
                      Height="25"
                      Margin="0,0,10,0"/>

            <Button Grid.Column="2"
                    Content="Test laden"
                    Click="ButtonTestLaden_Click"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="GebruikerId:"/>

            <TextBox Grid.Column="1"
                     Name="TextBoxGebruikerId"
                     Height="25"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Antwoorden:"/>

            <TextBox Grid.Column="1"
                     Name="TextBoxAntwoorden"
                     Height="25"/>
        </Grid>

        <TextBlock Grid.Row="4"
                   Text="Bv. ADEC"
                   Margin="120,0,0,10"/>

        <TextBox Grid.Row="5"
                 Name="TextBoxTest"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"/>

        <Grid Grid.Row="6" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1"
                    Content="Sluiten"
                    Margin="5"
                    Click="ButtonSluiten_Click"/>

            <Button Grid.Column="2"
                    Content="Verbeteren"
                    Margin="5"
                    Click="ButtonVerbeteren_Click"/>
        </Grid>
    </Grid>
</Window>