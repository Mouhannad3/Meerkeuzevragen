<Window x:Class="UI.TestExporterenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Test exporteren" Height="190" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Test exporteren naar txt"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0"
                   Content="Test:"
                   Width="80"/>

            <ComboBox Grid.Column="1"
                      Name="ComboBoxTesten"
                      Height="25"/>
        </Grid>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Annuleren"
                    Width="90"
                    Margin="5"
                    Click="ButtonAnnuleren_Click"/>

            <Button Content="Exporteren"
                    Width="90"
                    Margin="5"
                    IsDefault="True"
                    Click="ButtonExporteren_Click"/>
        </StackPanel>
    </Grid>
</Window>
