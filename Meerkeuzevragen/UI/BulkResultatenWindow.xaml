<Window x:Class="UI.BulkResultatenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bulk resultaten verwerken"
        Height="260"
        Width="520">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Test:"/>
        <ComboBox Grid.Row="0" Grid.Column="1" Name="ComboBoxTesten"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Bestandspad:"/>
        <TextBox Grid.Row="1" Grid.Column="1" Name="TextBoxPad"/>

        <Button Grid.Row="3"
                Grid.Column="0"
                Content="Verwerken"
                Width="110"
                Click="ButtonVerwerken_Click"/>

        <Button Grid.Row="3"
                Grid.Column="1"
                Content="Sluiten"
                Width="110"
                HorizontalAlignment="Right"
                Click="ButtonSluiten_Click"/>
    </Grid>
</Window>